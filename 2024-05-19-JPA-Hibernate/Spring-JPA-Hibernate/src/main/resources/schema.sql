create TABLE course(
	id int not null, 
	name VARCHAR(255) not NULL,
	author VARCHAR(255) not NULL,
	PRIMARY key (id)
);

create TABLE Course_Details(
	id int not null, 
	name VARCHAR(255) not NULL,
	author VARCHAR(255) not NULL,
	PRIMARY key (id)
);